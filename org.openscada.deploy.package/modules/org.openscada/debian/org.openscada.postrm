#! /bin/sh

OS_USER=openscadad

set -e

case "$1" in
    purge)

		if which deluser >/dev/null 2>&1; then
			deluser --quiet "$OS_USER" > /dev/null || true
		fi

        rm -r "/var/lib/$OS_USER"
        ;;
esac

#DEBHELPER#

exit 0
