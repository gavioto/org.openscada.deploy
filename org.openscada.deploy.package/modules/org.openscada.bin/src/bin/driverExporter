#!/bin/bash

. th4Functions

INST="$1"
shift

cfg="/etc/openscada"
if [ -z "$TH4_CONFIGURATION_ROOT" ]; then
	cfg="$TH4_CONFIGURATION_ROOT"
fi

th4Service org.openscada.da.utils.daemon.ExporterDaemon "$INST" "$cfg/drivers/$INST/exporter.xml" `user` "$@"
 